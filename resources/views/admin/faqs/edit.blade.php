<x-site-layout><section class="mx-auto max-w-2xl px-6 py-16 sm:px-10"><div class="rounded-3xl border border-rose-100 bg-[#fff8ee] p-8 shadow-sm"><h1 class="text-3xl font-semibold text-slate-900">Edit FAQ</h1><form method="POST" action="{{ route('admin.faqs.update', $faq) }}" class="mt-8 grid gap-5">@csrf @method('PUT')<select name="category_id" required class="rounded-xl border-rose-100 bg-white px-4 py-3">@foreach ($categories as $category)<option value="{{ $category->id }}" @selected($faq->category_id === $category->id)>{{ $category->name }}</option>@endforeach</select><input name="question" value="{{ $faq->question }}" required class="rounded-xl border-rose-100 bg-white px-4 py-3"><textarea name="answer" required rows="5" class="rounded-xl border-rose-100 bg-white px-4 py-3">{{ $faq->answer }}</textarea><button class="justify-self-start rounded-full bg-slate-900 px-6 py-3 text-sm font-semibold text-white">Save FAQ</button></form></div></section></x-site-layout>
